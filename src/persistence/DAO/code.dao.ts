import { SQLManager } from "../managers/SQLManager";

export  class CodeManager extends SQLManager {
    constructor(options:any,tableName:string){
        super(options,tableName)
    }
    
   
    
}